language: python
python:
    - "3.5"
    - "3.6"
    - "3.7"
    - "3.8"

install:
  - pip install -r requirements.tx

branches:
    only:
    - master
    - develop

notifications:
  email: false

script:
  python setup.py test
